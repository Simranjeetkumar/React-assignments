import React from 'react'
import AddIcon from '@mui/icons-material/Add';

const MuiPlus = (props) => {
    const {...rest} = props;
  return (
    <>
    <AddIcon {...rest}/>
      
    </>
  )
}

export default MuiPlus
